
const getColumn = boardingPass => {
    return 0;
};

const getRow = boardingPass => {
    return 0;
};

const getSeatId = boardingPass => {
    return 0;
};

const scan = boardingPasses => {
    return 0;
};

module.exports = { getColumn, getRow, getSeatId, scan };

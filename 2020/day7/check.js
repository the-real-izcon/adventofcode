const MY_BAG = 'shiny gold';

const buildBagData = bagRules => {
    return {};
};

const getPossibleColors = (bagData, thisColor, foundColors) => {
    return [];
};

const checkRules = bagRules => {
    return 0;
};

module.exports = { buildBagData, checkRules, getPossibleColors };

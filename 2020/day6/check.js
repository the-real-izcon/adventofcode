
const getUniqueAnswers = answers => {
    return [];
};

const getAnswersCount = answers => {
    return 0;
};

const checkCustoms = answersList => {
    return 0;
};

module.exports = { checkCustoms, getAnswersCount, getUniqueAnswers };
